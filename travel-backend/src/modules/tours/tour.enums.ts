export enum TourStatus {
    DRAFT = 'draft',
    PUBLISHED = 'published',
    CLOSED = 'closed',
}

export enum TourPackageStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}

export enum TourDepartureStatus {
    DRAFT = 'draft',
    OPEN = 'open',
    FULL = 'full',
    CLOSED = 'closed',
    CANCELLED = 'cancelled',
}

export enum TransportType {
    BUS = 'bus',
    LIMOUSINE = 'limousine',
    FLIGHT = 'flight',
    SELF_ARRIVAL = 'self_arrival',
}

export enum DepartureOptionStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}